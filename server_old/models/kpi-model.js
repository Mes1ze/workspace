const query = require('../database/query')

class KpiModel {
    async kpiFromTask(req) {
       
    }

    async kpiForWorker(req) {
        
    }

    async kpiForAllWorkers(req) {
        
    }
}

module.exports = new KpiModel()
